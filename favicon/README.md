# favicon/

Coloque aqui os favicons do projeto (um gerador como realfavicongenerator.net entrega tudo):

- `favicon.ico`
- `favicon.svg`
- `favicon-96x96.png`
- `apple-touch-icon.png`
- `site.webmanifest`

Os `<link>` no `<head>` do `index.html` já apontam pra esses nomes.
O `.cpanel.yml` já copia a pasta `favicon` inteira no deploy.
